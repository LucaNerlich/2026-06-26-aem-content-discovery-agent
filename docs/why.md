# Why certain decisions were made

Append-only decision log for the AEM Content Discovery Agent project. Every entry records the context, the choice, the alternatives that were ruled out, and the consequences. Entries are ordered from most foundational (architecture, stack) to most local (per-stage tuning).

## 2026-06-26 - Decision Log Rule
**Context:** A multi-wave plan with many implementors needs a shared memory of why each non-trivial choice was made, or the project re-litigates the same trade-offs every wave.
**Decision:** Repo-root `why.md` is the canonical decision log. Every implementor whose work makes or executes a non-trivial decision MUST append a dated entry before marking their task complete. Append-only; never edit prior entries.
**Alternatives considered:** Inline rationale in commit messages (rejected - not greppable, not co-located with the spec); a separate ADR directory (rejected - overkill for a single-author exercise); skip it (rejected - the brief explicitly grades on "justifying decisions").
**Consequences:** Future maintainers (and the interview reviewer) can answer "why this?" without re-reading the whole spec. Costs roughly 60 seconds per decision.

## 2026-06-26 - Architecture: JSON-primary, AEM-optional
**Context:** The original draft assumed the agent reads fragments live from AEM at runtime. A reviewer who cannot run the local AEM SDK (30+ minute install) would then be unable to run the agent at all, violating the brief's "single-command invocation" requirement.
**Decision:** `data/corpus.json` is the canonical, committed source of truth. The agent reads it by default. AEM is a code path behind two flags: `seed --aem-push` (writes fragments into AEM) and `agent --source=aem` (reads fragments live from AEM).
**Alternatives considered:** Live AEM only (rejected - reviewer-hostile, fragile); AEM-only with a JSON snapshot for backup (rejected - same fragility, more code); JSON only and drop AEM entirely (rejected - loses the AEM-depth interview signal).
**Consequences:** Reviewer can `npm run agent` on a clean clone with zero AEM dependency. The AEM code path still ships and is tested, demonstrating AEM competence as an opt-in capability. Cost: two `FragmentSource` implementations instead of one.

## 2026-06-26 - npm workspaces monorepo with shared / content-seeder / discovery-agent
**Context:** The seeder and the agent both need the same Zod schemas, Ollama wrappers, and AEM client. They have different CLIs, different dependencies (seeder needs faker, agent does not), and different lifecycles (seeder runs once, agent runs many times).
**Decision:** npm workspaces with three packages - `shared/` (schemas, Ollama, AEM, retrieval primitives), `content-seeder/` (the seed script), `discovery-agent/` (the runtime CLI + pipeline).
**Alternatives considered:** Single package with everything under `src/` (rejected - couples seeder-only deps onto the agent and vice versa); three separate git repos (rejected - code review and reproducibility nightmare for an 8h exercise); pnpm workspaces (rejected - adds a tool the reviewer must install when npm ships with Node).
**Consequences:** Clean dependency boundaries; the agent's `node_modules` does not pull in faker. Costs: marginally more `package.json` files; first-time contributors must remember `npm install` at the root, not inside a sub-package.

## 2026-06-26 - LLM stack: gemma4:26b chat + embeddinggemma:300m embeddings, both via local Ollama
**Context:** The brief requires justifying the embedding model AND a generation model. Locale coverage spans en-gb / fr-fr / de-de.
**Decision:** Chat = `gemma4:26b` (released Apr 2026, MoE ~3.8B active / 25.2B total, 256K context, native function calling, configurable thinking mode). Embeddings = `embeddinggemma:300m` (768d default, Matryoshka-truncatable to 512/256/128, 100+ languages, ~600 MB RAM). Both served by local Ollama at `http://localhost:11434`.
**Alternatives considered:** `gemma3:27b` + `nomic-embed-text` (rejected - nomic is English-mostly, weak for fr-fr/de-de); OpenAI hosted (rejected - brief explicitly favours local models; also costs and rate limits); single model for both (impossible - chat models are not good embedders).
**Consequences:** Single Gemma research lineage for both stages = cleaner architecture narrative. Multilingual retrieval works out of the box. Matryoshka gives a credible scale-up story (768→256 for 3× memory savings at hypothetical 40k-doc scale). Cost: gemma4:26b is ~18 GB on disk; reviewers on low-RAM hardware need a fallback model via env var.

## 2026-06-26 - sqlite-vec for persistent vector storage
**Context:** The agent needs a vector index. Corpus size is ~120 fragments now, hypothetically 40k+. Re-embedding on every run wastes 10+ minutes.
**Decision:** Use the `sqlite-vec` extension loaded into `better-sqlite3`, persisted at `data/embeddings.db`. Lexical search uses `wink-bm25-text-search` in-memory.
**Alternatives considered:** In-memory brute-force cosine in pure JS (rejected - re-embeds every run, no persistence story); `hnswlib-node` (rejected - native build complexity, no SQL interface for inspection); `lancedb` (rejected - heavier than needed at this scale); dedicated vector DB server like Qdrant (rejected - zero-server constraint).
**Consequences:** Real persistent vector store, SQL-queryable for debugging, zero-server. Demonstrates picking a right-sized tool. Cost: `better-sqlite3` + `sqlite-vec` add native dependencies that compile on install.

## 2026-06-26 - No ESLint, no Prettier, rely on `node --check` only
**Context:** This is a single-author 8h exercise. Configuring ESLint/Prettier for ESM + the workspace layout eats time that should go into the agent itself.
**Decision:** No linter, no formatter. `node --check` on every source file during CI/local verification catches syntax errors. Code style is enforced by review, not tooling.
**Alternatives considered:** Full ESLint + Prettier setup (rejected - 30+ min of yak shaving for a one-shot project); just Prettier (rejected - same setup cost, marginal benefit for one author); typescript-eslint (rejected - project is JS, not TS).
**Consequences:** Faster iteration, fewer config files. Costs: no automatic style enforcement; reviewers must trust the author's hand.

## 2026-06-26 - AEM Assets API verified shape (brandGuidelinesApplied as JSON array, lastModified as `{value, type:'calendar'}`)
**Context:** The Assets API's `.json` and `.model.json` representations of Content Fragment fields are not symmetric between read and write, and Adobe's docs do not spell out the exact wire format for multi-value fields on read.
**Decision:** `brandGuidelinesApplied` is read as a JSON array of strings (despite being stored as a Granite multi-value property). `lastModified` is read as `{ value: "<ISO string>", type: "calendar" }`. Both shapes were verified end-to-end by Task 2's smoke test before Task 5 was locked.
**Alternatives considered:** Assume the shapes from Adobe docs (rejected - docs are incomplete on this); use Sling GraphQL instead of Assets HTTP API (rejected - heavier, also under-documented for this exact case); parse permissively and accept anything (rejected - loses schema-driven safety).
**Consequences:** The AEM client's read path now has a verified contract; the Zod schema enforces it. Cost: ~30 min of smoke-testing during Task 2 to nail the shape.

## 2026-06-26 - Maven profile is `autoInstallPackage` (not `autoInstallSinglePackage`)
**Context:** The initial spec referenced `mvn install -PautoInstallSinglePackage` for pushing the CF Model to local AEM. Task 2 discovered the actual `aemcontentdisc/pom.xml` declares `autoInstallPackage`, not the `Single` variant.
**Decision:** Use `mvn install -PautoInstallPackage` for the local install. Documented in README and the seeder's prerequisite check.
**Alternatives considered:** Add a `autoInstallSinglePackage` profile to the pom to match the docs (rejected - modifies pre-existing AEM project beyond what's needed); both (rejected - confusing). Fixing the docs is correct.
**Consequences:** Reviewers running the optional AEM path get a command that actually works. Cost: one line change in instructions; no code change.

## 2026-06-26 - Ollama Error Model: 7 typed classes with selective retry
**Context:** Ollama can fail in at least seven distinct ways (down, 5xx, timeout, model missing, malformed JSON, context overflow, programmer error). A single `OllamaError` class loses the information needed to decide whether to retry, surface, or re-prompt.
**Decision:** Define 7 typed classes - `OllamaUnavailableError`, `OllamaServerError`, `OllamaTimeoutError`, `OllamaModelNotFoundError`, `OllamaJsonParseError`, `OllamaContextOverflowError`, `OllamaInvariantError`. Inside `ollamaFetch`, only `Unavailable` and `Server` are retried (with backoff); everything else surfaces immediately. `JsonParse` retry is the CALLER's responsibility - the caller re-prompts with the parse error appended to the system prompt.
**Alternatives considered:** Single error class (rejected - loses retry semantics); retry everything (rejected - model-not-found and context-overflow will never succeed on retry, just burn time); no retry at all (rejected - transient 502s from Ollama under load are real).
**Consequences:** Pipeline stages can `catch` exactly the error class they know how to handle. Cost: 7 classes to maintain; 7 distinct test cases.

## 2026-06-26 - Uniform LLM retry pattern at the caller layer
**Context:** Every LLM-using pipeline stage (rewrite, gap, compose, match-reason) wants the same behaviour: if the model returns malformed JSON or the parsed object fails Zod, re-prompt once with the error appended; otherwise let typed errors propagate.
**Decision:** All LLM-using pipeline stages use the same retry pattern - catch `OllamaJsonParseError` and `ZodError` once, append the error message to the system prompt, retry exactly once. Let every other typed error propagate (because `ollamaFetch` already retried the transient ones).
**Alternatives considered:** Per-stage bespoke retry (rejected - four near-identical implementations that drift); retry N>1 times (rejected - if the model can't produce valid JSON on the second try given the explicit error, it won't on the third either); no retry (rejected - observed JSON-mode flakiness with gemma4:26b makes one retry valuable).
**Consequences:** One retry helper used by all stages; consistent observability. Cost: callers must know to construct the system prompt as a function of `errorContext`.

## 2026-06-26 - prompt-log.md logs every call, success or failure
**Context:** The brief requires submitting prompt logs. Logging only successes hides the failure modes that matter most for debugging and architecture review.
**Decision:** `prompt-log.md` records every Ollama call with `{ ts, model, mode, ok, errorClass?, elapsedMs, promptHead, responseHead }`. `promptHead` and `responseHead` are 200-char truncations. Local-only tool, so the truncated heads are kept (not redacted).
**Alternatives considered:** Log successes only (rejected - failures are the interesting telemetry); full prompt + full response (rejected - bloats the log and the response can be tens of KB); no log at all (rejected - brief requires it).
**Consequences:** The log is human-readable, greppable, and small. Costs: 200 chars is sometimes not enough to see the failure cause; in those cases the implementor adds a one-off `console.error` rather than expanding the log format.

## 2026-06-26 - Seeder owns ALL embedding writes (agent is read-only)
**Context:** Two writers to `data/embeddings.db` would race on first run and produce subtle dimension mismatches if the agent ever re-embedded a fragment with a different model version than the seeder used.
**Decision:** The seeder is the sole writer to `data/embeddings.db`. Each seed run drops and recreates the embeddings table - all-or-nothing seeding. The agent opens the DB read-only.
**Alternatives considered:** Agent re-embeds on cache miss (rejected - races + dim drift); incremental seeding with cache key `(id, lastModified, model)` (rejected - adds schema we don't need yet; will be added if/when incremental seeding becomes a real requirement); no embeddings DB, embed-on-startup (rejected - 30s+ cold start on every agent run).
**Consequences:** Predictable: agent startup is fast and deterministic. Seeder runs are self-contained. Cost: any fragment change requires a full re-seed.

## 2026-06-26 - No runtime embedding cache in the agent
**Context:** Carrying a cache-key schema (id, lastModified, model) when the seeder is the only writer and seeds atomically introduces dead schema and one more invalidation surface.
**Decision:** No runtime embedding cache. The seeder drops + recreates the table; the agent reads the result. If incremental seeding ever becomes a need, the cache key gets added then - not before.
**Alternatives considered:** Build the cache key now "for future-proofing" (rejected - YAGNI, and unused schema is a maintenance trap); cache embeddings in memory inside the agent (rejected - pointless, the DB read is already in-process and microsecond-scale).
**Consequences:** Smaller schema, fewer invariants to test. Cost: a future "only re-embed changed fragments" feature will require a small migration; acceptable because that feature does not exist yet.

## 2026-06-26 - Default corpus size: 120 fragments (40 per locale × 3 locales)
**Context:** Earlier draft said 24 fragments total (8 per locale). With three locales, three categories, four brand-guideline combinations, and a deliberate planted gap, 24 is too thin - retrieval becomes trivially correct or trivially empty, eval signal is noisy.
**Decision:** Default corpus = 120 fragments. 40 per locale × 3 locales. Seeder `--count` flag still allows scaling down for fast local iterations.
**Alternatives considered:** Keep 24 (rejected - too small to discriminate retrieval quality); 300+ (rejected - seeding runtime becomes prohibitive at ~20-30s per LLM-generated body); 60 (rejected - still thin for the eval harness's precision/recall to be meaningful).
**Consequences:** More realistic retrieval signal; meaningful eval precision@3 and recall@3 numbers. Cost: ~40-60 minutes one-time seeding wall-clock against local Ollama.

## 2026-06-26 - Pure random topic distribution + guaranteed-coverage subset
**Context:** If all fragments are drawn from a small topic pool, retrieval has nothing to discriminate. If all topics are pure random, the demo brief (winter-sustainable) may match nothing and the demo falls flat.
**Decision:** Each locale gets ≥6 fragments drawn from a locked "seasonal-clothing-sustainability" topic pool (guaranteed coverage for the demo brief), and the remaining ~34 fragments per locale are pure random topics from the broader category pool.
**Alternatives considered:** Pure random everywhere (rejected - demo brief might find zero matches); pure curated for the demo brief (rejected - retrieval becomes trivial); topic-balanced quotas across all categories (rejected - too much hand-engineering, defeats the "honest retrieval" signal).
**Consequences:** Demo always has plausible matches; retrieval still has to work against ~85% random distractors. Cost: the locked topic pool must be maintained as a constant in the seeder.

## 2026-06-26 - Retrieval scoring weights: 0.6 cosine / 0.3 BM25 / 0.1 freshness
**Context:** The corpus is LLM-paraphrased marketing copy. Pure BM25 misses paraphrasing; pure cosine misses brand and material proper nouns ("merino", brand names). Freshness has signal but should not dominate.
**Decision:** Final score = 0.6 × cosine + 0.3 × BM25 + 0.1 × freshness. Semantic dominates because the corpus is paraphrased; BM25 is a backstop for proper-noun matching; freshness is a tiebreaker only.
**Alternatives considered:** 0.5/0.5 vector + BM25 (rejected - over-weights lexical against paraphrased prose); 1.0 cosine (rejected - misses exact brand matches); reciprocal rank fusion instead of weighted sum (rejected - RRF requires tuning a `k` constant, more knobs than this corpus justifies); learn the weights from the eval set (rejected - overkill for a 5-brief eval).
**Consequences:** Defensible weights with a clear story for the architecture doc. Cost: weights are constants; if the corpus character changes substantially (e.g. add long-form technical docs) they will need re-tuning.

## 2026-06-26 - Brief parser locale precedence: URL/path regex > LLM inference > en-gb default
**Context:** Task 7 (parseBrief) needs a locale on the StructuredBrief, but free-form briefs supply it inconsistently - sometimes as a path (`/en-gb/...`), sometimes only as audience text ("UK market"), sometimes not at all.
**Decision:** Three-tier precedence inside `parseBrief`. (1) Regex `/(en-gb|fr-fr|de-de)/` against the raw text wins absolutely and overrides whatever the LLM returns; mismatches are noted in `brief.uncertain`. (2) If no path locale, the LLM infers from the audience description. (3) If the LLM returns something outside the locked set, default to `en-gb` and record the fallback in `brief.uncertain`. `uncertain` is a new optional `string[]` on `StructuredBrief`.
**Alternatives considered:** Let the LLM be authoritative always (rejected - observed gemma4:26b drift on locale when audience country is implicit); regex-only (rejected - fails on briefs that describe audience without a path, including realistic marketing briefs); throw on ambiguity (rejected - non-interactive CLI must proceed; the brief explicitly favours "proceed + note" per spec §2d).
**Consequences:** Deterministic locale extraction when a path is present; transparent fallback signal when not. Downstream consumers (retrieval locale filter, gap analyser, composer) can trust `brief.locale` AND inspect `brief.uncertain` to know whether to surface the assumption in the user-facing output.

## 2026-06-26 - Locale relaxation ladder: exact → prefix (en-*) → any
**Context:** A brief tagged `en-gb` should prefer en-gb fragments, but if there are none, en-us is a better fallback than fr-fr or de-de. Forcing exact locale match can produce empty top-3 results in cross-locale gap scenarios.
**Decision:** Retrieval tries exact locale first. If too few results, relax to the language prefix (`en-*` matches both en-gb and en-us). If still too few, relax to any locale. Every relaxation step sets a `localeRelaxed` flag on the result so the composer and CLI can call it out in the output.
**Alternatives considered:** Exact locale only (rejected - produces empty results on small corpora); always search across all locales and post-filter (rejected - embedding-time cost wasted on locales that get discarded); user-specified relaxation policy (rejected - extra CLI surface that almost nobody will tune).
**Consequences:** Robust to thin per-locale coverage; transparent to the user that relaxation happened. Cost: three retrieval passes worst case (in practice one or two).

## 2026-06-26 - sqlite-vec rowid binding uses BigInt
**Context:** During Wave 3 seeder implementation, inserting embedding rows into the `fragments_vec` virtual table failed with a vec0 affinity error.
**Decision:** Cast all rowids to `BigInt` before binding them to better-sqlite3 prepared statements (`stmt.run(BigInt(rowid), …)`).
**Alternatives considered:**
- Use a TEXT primary key on `fragments_meta` and join by id only - rejected because sqlite-vec's k-NN query returns rowids, and joining via int rowid is cheaper than a TEXT lookup.
- Use a different vector library (sqlite-vss, hnswlib) - rejected because sqlite-vec is the locked choice (see entry above on persistent vector storage).
**Consequences:** Anyone writing to `fragments_vec` must remember the BigInt cast. Documented inline in `content-seeder/src/embeddings.js` so the next maintainer doesn't trip on it.

## 2026-06-26 - Gap analyser: single batched LLM judge + deterministic suggestedAction + force-none invariants
**Context:** Task 9 needs to classify each required topic in the brief as `none` or `partial` against the retrieval candidate pool, surface structural gaps (locale relaxation, missing brand voice), and produce a useful `suggestedAction` per gap. Multiple plausible designs (LLM-per-topic, LLM-everything, hand-rolled coverage rules) trade cost, determinism, and quality against each other.
**Decision:** (1) **Single batched LLM call** over all required topics; input = brief + JSON-serialised candidate pool (matches ∪ nearMisses ∪ droppedByBrandFilter). (2) **`suggestedAction` is deterministic**, derived from the topic words (mapped to seeder categories) + brief locale + brief `brandGuidelines` - the LLM has no influence on it. (3) **Hard invariants enforced post-LLM**: any `partialMatches` id not in the pool is dropped; `partial` with empty `partialMatches` is downgraded to `none`; `none` is forced when the pool is empty. (4) **Structural gaps (locale, brand) are synthesised independently** of the LLM verdicts, so they always appear when the retrieval signal demands them.
**Alternatives considered:** One LLM call per topic (rejected - N×latency for no recall benefit at the corpus size); ask the LLM to also generate `suggestedAction` (rejected — drift undermines test stability and the seeder's known vocabulary is the authoritative input anyway); compute coverage purely from retrieval scores (rejected — score thresholds are corpus-specific and noisy, and the user-visible `description` needs natural-language judgement); trust the LLM's `partialMatches` without sanitising (rejected — hallucinated ids would violate the contract's "no orphan ids" invariant).
**Consequences:** One Ollama chat call per agent run for gaps (cheap, ~1-3 s on local gemma4:26b). Test fixtures stay deterministic because suggestedAction is pure-functional. Locale/brand structural gaps are reliable regardless of LLM mood. Retry policy follows the Task 4 Ollama Error Model: `OllamaJsonParseError` or `ZodError` → one retry with the failure appended to the system prompt, then propagate.


## 2026-06-26 — Cosine via `vec_distance_cosine()` (not vec0 MATCH)
**Context:** Task 8 needed to query `fragments_vec` (sqlite-vec virtual table) for nearest-neighbour search. sqlite-vec exposes both a vec0 `MATCH ?` k-NN operator and the scalar `vec_distance_cosine()` function.
**Decision:** Query `fragments_vec` with `vec_distance_cosine(embedding, ?)` rather than the vec0 `MATCH ?` k-NN operator. Score normalisation is `1 - distance`, clamped to [0,1].
**Alternatives considered:** vec0 `MATCH ?` — rejected because the score semantics aren't part of the stable sqlite-vec API; gives full control over the normalisation by computing the cosine score in-query.
**Consequences:** Queries scan the whole virtual table rather than using the ANN index, which is fine at corpus sizes ≤10k (target hypothetical 40k might warrant a revisit). Score formula is one source of truth and trivially testable.

## 2026-06-26 — BM25 <3-doc consolidation skip
**Context:** `wink-bm25-text-search` refuses to consolidate an index containing fewer than 3 documents and throws. Toy fixtures (a single locale, a 2-fragment proof corpus) blow up before retrieval even runs.
**Decision:** The retrieval layer catches the wink-bm25 "<3 documents" error from `consolidate()` and returns `[]` for the BM25 stage when the candidate pool is tiny. The vector path still runs normally.
**Alternatives considered:** Pad the index with synthetic documents (rejected — pollutes scores); patch wink-bm25 (rejected — vendored dependency, upstream); fail the run (rejected — small corpora are a real dev workflow).
**Consequences:** BM25 contribution is zero when the candidate pool (after locale + brand filters) has <3 fragments. Acceptable because vector dominates the final score (0.6 weight) and the planned realistic corpus is 120+ fragments. Tests and 2-fragment smoke runs continue to work.

## 2026-06-26 — AEM source → BM25-only fallback (no vector stage)
**Context:** `AemFragmentSource` reads fragments live from the AEM Assets API at runtime. It doesn't carry embeddings; only the seeder writes `data/embeddings.db`. Re-embedding on every agent run would cost 30+ s per query topic.
**Decision:** When `--source=aem`, the retrieval layer skips the vector stage entirely (`vectorSearchAvailable=false`) and uses BM25 + freshness only. The JSON source still uses the full cosine + BM25 + freshness blend.
**Alternatives considered:** Re-embed fragments on AEM load (rejected — 30+ s per topic on cold cache, defeats the demo); pre-warm an in-memory vector cache against the live AEM (rejected — first run is still slow and the cache is stale by design); reject `--source=aem` (rejected — loses the AEM-depth signal the brief grades on).
**Consequences:** `--source=aem` is a degraded retrieval mode useful for "does the live AEM have the content?" inspection; default `--source=json` is the demo path. The composer + gap analyser tolerate both modes because they receive a normalised `RetrievalResult` regardless.

## 2026-06-26 — Composer design decisions (Task 10)
**Context:** The composer is the last LLM stage and produces the `draftOutline` portion of `AgentOutput`. Two invariants matter most: (a) reuse sections only reference ids the user can see in `matchedFragments`; (b) the brief's `pathHint` is authoritative.
**Decision:**
- **Orphan-id check via Zod `superRefine`** — the schema (built dynamically per call from `matchedFragments` ids) rejects any reuse section whose `fragmentIds` contain an id outside that set. Implemented as `superRefine` rather than post-validation walks so invariants stay co-located with the schema (one source of truth).
- **`brief.pathHint` unconditionally overrides LLM-generated path** — even when the LLM returns a plausible value. Reason: the brief is the user's spec; the LLM's guess is advisory. Avoids drift across runs of the same brief.
**Alternatives considered:** Walk the parsed object after `.parse()` to check ids (rejected — invariant lives in two places, easy to drift); let the LLM pathHint win when it looks "good" (rejected — needs subjective heuristics, non-deterministic output).
**Consequences:** Hallucinated fragment ids fail validation with a clear path (`sections[N].fragmentIds[M]`) and trigger the standard one-retry pattern. Same brief → same pathHint across runs.

## 2026-06-26 — CHAT_TIMEOUT_MS bumped to 120s
**Context:** Wave 4 verifier hit timeout-then-propagate on the gap-analyser stage (largest batched prompt) with the 60s default during E2E smoke.
**Decision:** Bump CHAT_TIMEOUT_MS from 60_000 to 120_000.
**Alternatives considered:** Per-stage timeouts (overkill — only gap analyser is large); shrink the gap-analyser prompt (cheaper input but reduces context the judge needs to be accurate).
**Consequences:** Pipeline tolerates slow Ollama responses; a true hang still surfaces in ≤2 min. If gap-analyser ever balloons (e.g. >20 topics), revisit.

## 2026-06-26 — DraftOutline.sections capped at 1..8
**Context:** Wave 4 verifier observed no length cap on the outline sections array.
**Decision:** Add `.min(1).max(8)` to the sections array in the AgentOutput Zod schema.
**Alternatives considered:** Cap at 6 (too tight for complex briefs); leave uncapped (no upper bound on LLM hallucination).
**Consequences:** Composer prompt should aim for 4-6 sections; if it ever generates >8, the existing retry-once pattern surfaces a ZodError on second failure for the caller to handle.


## 2026-06-26 — Wave 4 smoke `matches=0` investigation: empty `requiredTopics` is the only mechanism that fits
**Context:** A Wave 4 E2E smoke against a 2-fragment `data/corpus.json` reported `retrieve()` returning `matches=0`. Four hypotheses were investigated offline with mocked embeddings (no live Ollama).
**Decision:** No code change. The findings:
- **BigInt rowid mismatch** — RULED OUT. A mocked embed that returns the stored vector for `frag_001` yields `cosine=1.0` for it on a fresh 2-row fixture DB built the same way the seeder builds it. The `fragments_vec` ↔ `fragments_meta` JOIN works.
- **Locale case mismatch (`en-gb` vs `en-GB`)** — RULED OUT. Exact-locale filter is case-sensitive, but the locale-ladder relaxes `en-GB → en-*` and still returns matches; `localeRelaxed="prefix"` is the only visible side effect.
- **Empty `requiredTopics`** — CONFIRMED as the only mechanism. With `requiredTopics: []` the per-topic loop body never runs, `perFragmentBest` stays empty, and `matches=0` is returned deterministically. Cannot verify whether the parser actually emitted `[]` during the live smoke without re-running Ollama, but no other path produces `matches=0` given a populated DB, a non-empty candidate pool after locale filtering, and a successful `embedImpl`.
- **Tiny-corpus / score-threshold artifact** — RULED OUT. `retrieve()` has no score threshold; vector hits at any score, plus freshness alone, are enough to populate `survived`. BM25 silently returns `[]` below 3 documents (see prior `BM25 <3-doc consolidation skip` entry) but vector still contributes.
**Alternatives considered:** Patch `retrieve()` to short-circuit log on empty topics (rejected — `retrieve()` is doing the right thing; the caller fed it an empty list). Tighten `StructuredBrief.requiredTopics` to `z.array(z.string()).min(1)` (deferred — would invalidate the parser's one-retry-on-ZodError path mid-Wave; revisit once smoke can be re-run end-to-end against Ollama to confirm parser emits `[]` rather than failing earlier).
**Consequences:** Smoke harness should log `brief.requiredTopics` (and `localeRelaxed`) on `matches===0` so the next occurrence is unambiguous in one read. Existing `shared/test/retrieve.test.js` exercises the small-corpus path (the `buildBm25Index` tests cover 0/1/3-doc cases); `discovery-agent/test/retrieve.test.js` covers the full 6-fragment retrieval. No regression test added because the only "failure" is the no-topics case, which is already an upstream contract issue and not a `retrieve()` bug.


## 2026-06-26 — StructuredBrief.requiredTopics tightened to min(1)
**Context:** Wave 4 E2E smoke surfaced matches=0 from retrieval. Path C investigation traced the root cause to parseBrief occasionally returning an empty `requiredTopics` array, which left retrieval with nothing to embed.
**Decision:** Tighten the Zod schema: `requiredTopics: z.array(z.string().min(1)).min(1)`. Outer min(1) forbids empty array; inner min(1) forbids empty strings.
**Alternatives considered:** Log-and-continue with a default topic ("the brief subject") — rejected because it silently corrupts retrieval results. Tightening fails fast and the existing retry-once pattern handles the recovery.
**Consequences:** Briefs that the model genuinely cannot parse into topics will fail with a clear ZodError on the second attempt; the caller surfaces a typed error instead of returning a hollow AgentOutput.

## 2026-06-26 — CLI uses dynamic imports + injectable pipeline deps
**Context:** Task 11 needs a `--quiet` flag that suppresses pino logs, but pino is constructed at module load time inside `shared/src/llm/ollama.js` from `process.env.LOG_LEVEL`. A static `import` of any module that transitively touches Ollama would freeze the log level before `--quiet` could take effect. Separately, the locked output contract requires the CLI smoke test to assert that `--json` output passes `AgentOutput.parse()` — exercising the full pipeline in a test would require live Ollama.
**Decision:** `discovery-agent/src/cli.js` parses `argv` first, sets `process.env.LOG_LEVEL = "silent"` when `--quiet` is set, then `await import(...)`s the pipeline stages and shared barrel. The CLI also exports a `runPipeline(brief, opts, deps)` function whose `deps` object can override `parseBrief`/`retrieve`/`analyseGaps`/`compose` for the smoke test, so the test asserts the AgentOutput contract without booting Ollama.
**Alternatives considered:** Set `LOG_LEVEL` in a shell wrapper before invoking node (rejected — moves a runtime contract out of the binary and breaks `node src/cli.js --quiet` invocation per the task). Inject the pino logger via DI everywhere (rejected — invasive refactor across `shared/`, outside Task 11 scope). Spawn the CLI in a child process for the smoke test against live Ollama (rejected — flaky CI, ~30s per run, and the failure mode already shows up in dev as "pipeline error" + exit 1).
**Consequences:** `--quiet` works without any env-var ceremony from the user. The smoke test is deterministic and fast (~5 ms) and still exercises the end-to-end Output schema. Cost: the CLI's pipeline-stage imports are dynamic, so a typo in a pipeline module surfaces only when the stage actually runs (acceptable — `node --test` against the pipeline modules catches that earlier).


## 2026-06-26 — Eval harness bypasses `runPipeline` so a `compose` failure does not zero the brief's metrics
**Context:** Task 12 calls for an offline evaluator that runs `compose(...)` end-to-end and prints precision@3 / recall@3 / gap-F1 per brief. The pipeline's final stage (`compose`) can fail Zod validation when the chat model emits a non-conformant `DraftOutline` (notably small fallback chat models). A naive `runPipeline` call would throw at compose-time and the brief would record zero scores even though `retrieve` and `analyseGaps` had already produced the data the metrics actually consume.
**Decision:** `eval/run.js` calls the pipeline stages individually (`parseBrief → retrieve → analyseGaps → compose`). `matchedFragments` are reconstructed from `retrieval.matches` using the exact projection `compose` uses, so the metrics match the AgentOutput contract. A compose failure is captured into `composeError` on the per-brief record but does not abort metric calculation. The per-brief printout and `eval/latest.json` both surface the compose failure when it happens, so the auditor sees the divergence.
**Alternatives considered:** Call `runPipeline` and treat any throw as a complete brief failure (rejected — loses the retrieval/gap signal the harness is supposed to score). Make `compose` itself tolerant of validation errors (rejected — invasive, breaks Task 10's fail-loud contract). Move `matchedFragments` projection into a shared helper (deferred — single use site today; can be hoisted into `shared` when a second caller appears).
**Consequences:** Eval results remain meaningful when the chat model occasionally drifts on the draft-outline shape. The trade-off is that `eval/run.js` knows the `matchedFragments` projection shape; if `compose` ever changes it, `eval/run.js` must be updated to match (caught by the AgentOutput schema in tests but not in the eval itself).

## 2026-06-26 — Eval harness honors `EVAL_CHAT_MODEL` without modifying `shared/`
**Context:** The agent's locked chat model is `gemma4:26b`. On hardware where it cannot finish a chat call inside the 120s timeout, every brief in the eval times out and the harness produces no metrics. The brief author cannot tune the runtime model without changing `shared/src/llm/ollama.js`, which is out of scope for Task 12.
**Decision:** `eval/run.js` reads `EVAL_CHAT_MODEL` (default = the shared `CHAT_MODEL` constant) and wraps `chat` so every pipeline-stage call defaults to that model. The shared `chat`/`CHAT_MODEL` defaults are untouched; only the harness's chat invocations move. The README documents the env var and warns that expectations are calibrated against `gemma4:26b` and that smaller fallbacks can flip topic coverage between `partial` and `none`.
**Alternatives considered:** Add `OLLAMA_CHAT_MODEL` support inside `shared/src/llm/ollama.js` (rejected — scope creep into another task's module; would also silently change the agent's runtime behavior). Hard-code a fallback model in the harness (rejected — graders should choose). Lower the default `EVAL_F1_THRESHOLD` to mask model variance (rejected — the threshold is the verification contract; the fallback model exit code is the user's signal to choose a stronger model, not to lower the bar).
**Consequences:** Graders on constrained hardware can still run the harness end-to-end and inspect `eval/latest.json`. The threshold check intentionally still fails when a weaker fallback model produces lower-quality verdicts, so the failing exit code is informative rather than misleading.

## 2026-06-26 — Config-driven model selection via `config/models.json`
**Context:** The model identifier lived as hard-coded `CHAT_MODEL` / `EMBED_MODEL` constants in `shared/src/llm/ollama.js`, with copies leaking into `eval/run.js`, the seeder, and pipeline stages. Swapping a model (for evals on weaker hardware, or to try a per-stage model like a smaller `parseBrief`) required editing source. Future per-stage A/B tests had no obvious seam.
**Decision:** Add `config/models.json` at repo root as the single source of truth, with `chat.default` plus optional per-stage overrides (`parseBrief`, `analyseGaps`, `compose`) and `embedding.default`. New loader `shared/src/config/models.js` exposes `getChatModel(stage)` and `getEmbeddingModel()`; the loader resolves the file relative to the loader module (via `import.meta.url`) so it works from any workspace cwd. `CHAT_MODEL` / `EMBED_MODEL` remain exported but are now computed from the loader, preserving every existing import. Pipeline stages (`parseBrief`, `analyseGaps`, `compose`) and the seeder pass their stage name through; `EVAL_CHAT_MODEL` env still wins over config in `eval/run.js`.
**Alternatives considered:** Environment variables only (rejected — five-plus envs to set, no per-stage shape, easy to forget); a JS module exporting constants (rejected — defeats the goal of "edit one file, no rebuild"); YAML (rejected — would add a parser dep when JSON.parse is built in); per-package config files (rejected — drift risk across `shared/`/`seeder/`/`agent/`).
**Consequences:** Single edit in `config/models.json` flips the model anywhere it is used; `npm test` still passes against the spec-locked `gemma4:26b` / `embeddinggemma:300m` defaults. Loader fails loudly with a clear path-aware error if the file or required defaults are missing, so silent drift onto a wrong model is impossible. Cost: one new module, one new test file, and a tiny amount of indirection at three pipeline call sites.

## 2026-06-26 — Docs split: README quick-start + `/docs/` deep-dives + structured prompt-log
**Context:** Task 13 needed to deliver project documentation that lands on a reviewer's first clone. The old root `readme.md` documented the AEM-only pre-archetype workflow, not the JSON-primary three-workspace architecture that actually shipped. The root `prompt-log.md` is an auto-appended runtime transcript (1500+ lines) — useful but not a designed document — and the spec called for a separate, hand-curated prompt-log that captures the verbatim system/user templates and the tuning iterations behind them.
**Decision:** Rewrote `README.md` (uppercase, via `git mv` because macOS HFS+ is case-insensitive) as the entry point: prerequisites → quick-start → architecture sketch → output contract → sample run pointer → eval results table → optional AEM round-trip → repo layout → short design notes → external docs. Added `docs/architecture.md` (Mermaid pipeline diagram, per-stage walkthrough, schema reference, vector-store + Matryoshka scaling note, Adobe MCP production-equivalent paragraph), `docs/sample-run.md` (real `--json` capture of `winter-sustainable.txt` plus the canonical Markdown render derived from the same JSON), and `docs/prompt-log.md` (verbatim system/user templates for every chat call plus the seeder's six voice templates, with start→end tuning notes per stage). Kept the existing root `prompt-log.md` as the runtime transcript and cross-linked it from `docs/prompt-log.md`.
**Alternatives considered:** One mega-README covering everything (rejected — buries the quick-start under reference material reviewers don't need on first read); one combined `docs/everything.md` (rejected — same problem; also makes `docs/sample-run.md`'s code-block JSON hostile to diff); regenerating the sample run from scratch with `gemma4:26b` (attempted — every attempt timed out at 120 s on the host hardware; restored `config/models.json` to the locked `gemma4:26b` default and captured the sample with `qwen2.5-coder:1.5b` instead, called out explicitly in `docs/sample-run.md`); deleting the auto-appended root `prompt-log.md` to avoid two files with the same name in different folders (rejected — it documents real chat behaviour and the chat wrapper writes to it on every invocation; deleting it would mean editing `shared/src/llm/prompt-log.js`, which is out of scope for a docs task).
**Consequences:** A fresh clone now leads with the JSON-primary quick-start that actually runs. The architecture, sample, and prompt-design docs are co-located under `docs/` for easy review. The sample run is a real pipeline output (not hand-crafted) but is captured against a smaller fallback model — graders re-running with `gemma4:26b` will see different outline rationale and may see one or two reuse sections instead of all-NEW; the schema, matched-fragment block, and structural gaps are stable. Cost: ~700 lines of documentation; one cross-link between the runtime `prompt-log.md` and the structured `docs/prompt-log.md` that future contributors must keep in mind.


## 2026-06-26 — Task 15 split into 15a (scaffold) / 15b (run) / 15c (commit)
**Context:** Task 15 originally bundled corpus seeding, an end-to-end demo run against `gemma4:26b`, and committing the resulting bundle into one implementor handoff. On the dev machine `gemma4:26b` regularly hits the 120 s chat timeout per call (~4 calls per brief × 20 briefs = ~32 calls), so the whole task would block the implementor on Ollama wall-clock rather than on actual code.
**Decision:** Split into three phases. 15a (this commit) scaffolds the runner: 3 new "PDF-task-style" briefs in `eval/briefs/` (one per locale), a `scripts/alpha-run.js` orchestrator wired up as `npm run alpha`, a placeholder `runs/alpha/README.md`, and unit tests for the orchestrator. 15b is the user running `npm run alpha` against a pre-warmed Ollama locally and capturing the JSON/Markdown/meta bundle. 15c is the commit of the produced artifacts. Implementor stays inside code-only scope.
**Alternatives considered:** Run everything inside the implementor session (rejected — Ollama timeouts on a single brief would burn the session budget without producing the artifacts the brief grades on); spawn a background process and poll (rejected — coordinator and user have no way to see partial progress from a background agent, and a single failure wedges the whole bundle); shrink to a faster fallback model (rejected — the brief explicitly grades the runtime against `gemma4:26b`; capturing a `qwen2.5-coder:1.5b` bundle would mislead the reviewer about output quality).
**Consequences:** The implementor delivers a deterministic, testable scaffold; the user owns the slow Ollama wall-clock; the commit step is a separate, small change once the bundle is on disk. Cost: two extra hand-offs between phases, but each is a single command.

## 2026-06-26 — Alpha runner: one retry per brief, never abort the whole run
**Context:** `gemma4:26b` chat calls intermittently fail with `OllamaJsonParseError`, `OllamaTimeoutError`, or rare schema drift. Without retry, a single transient failure would zero the output for that brief; with unbounded retry, one stuck brief could blow the entire run's wall-clock past any reasonable budget.
**Decision:** `runBriefWithRetry` retries exactly once per brief on any thrown error, then records `status: "ok" | "timeout" | "error"` in the brief's `.meta.json` and continues to the next brief. The whole-run loop never re-throws — exit code is always 0 and the user triages from the generated `runs/alpha/README.md`. Timeout is classified separately from generic error via a message regex (`/timeout|timed out|OllamaTimeoutError/i`) so the index README can report it distinctly.
**Alternatives considered:** Retry until success (rejected — a stuck call burns wall-clock with no upper bound); no retry (rejected — observed gemma4:26b JSON-mode flakiness wastes good briefs on transient parse drift, same justification as the Task 4 retry pattern); per-stage retry inside the orchestrator (rejected — pipeline stages already retry once on `OllamaJsonParseError`/`ZodError`; double-retry would mask real failures).
**Consequences:** Total run wall-clock has a hard ceiling of `2 × per-brief budget × brief count`. The bundle always exists and is honest about what failed. Cost: a brief that fails twice produces only `.meta.json` (no `.json` / `.md`) — readers must check status before parsing.

## 2026-06-26 — Three alpha-runner briefs have no `eval/expectations/` entry
**Context:** The 3 PDF-task-style briefs (`en-gb-spring-rewear`, `fr-fr-loungewear-premium`, `de-de-workwear-tech`) are designed to exercise distinct gap patterns for the alpha-run demo bundle, but the corpus was not seeded with those topics in mind. Hand-labelling expectations without the LLM-judge feedback loop the original 5 briefs went through would produce uncalibrated `expectedMatchIds` and `expectedGaps` arrays.
**Decision:** Ship the three briefs in `eval/briefs/` (so the alpha runner picks them up automatically) but do NOT add matching files under `eval/expectations/`. The eval harness (`eval/run.js`) now warns and skips briefs without expectations rather than throwing, so `npm run eval` still scores the calibrated 5. The README's "Per-brief scenarios" table is unchanged; a one-liner above it documents the skip behaviour.
**Alternatives considered:** Hand-label expectations from a single eyeballed run (rejected — would skew aggregate precision/recall/gap-F1 with un-calibrated labels and break the threshold contract); move the new briefs into a `eval/briefs-alpha/` directory (rejected — duplicates the file-discovery code in two places and breaks the "alpha runner reads everything in `eval/briefs/`" rule); add expectations later (deferred — easy to do once the alpha bundle reveals what the model actually returns).
**Consequences:** `npm run eval` aggregate metrics stay calibrated; the alpha bundle still demonstrates the agent on three brand-new content angles. Cost: two slightly different "what counts as a brief" rules — but the warn-and-skip surface is one line of stderr that makes the asymmetry visible on every eval run.

## 2026-06-27 — Phase 15a-fix-2: accommodate qwen3 thinking with per-stage `num_predict` caps and a model-agnostic think-stripper
**Context:** First alpha against `qwen3.5:9b` produced a single fragment of ~5000 words at 6730 eval-tokens for a 250-word target (no `num_predict` cap anywhere) and Fragment 2 then died after 105 s with `OllamaInvariantError("empty or malformed response")` because the model truncated mid-`<think>`. Root cause: `qwen3.5` emits a `<think>...</think>` block before the user-facing reply, and (a) the seeder system prompt was too loose to constrain length, (b) every chat call relied on Ollama's default `num_predict` which is unbounded enough to run away on small targets but small enough to truncate mid-think on big ones, and (c) downstream parsers received the raw `<think>` prefix and either choked on JSON or wasted budget rendering thoughts as prose.
**Decision:** ACCOMMODATE thinking, do not disable it. Five surgical changes: (1) `shared/src/llm/chat.js` unconditionally strips a leading `<think>...</think>` block (regex `/^\s*<think>([\s\S]*?)<\/think>\s*/i`) before the empty-check and JSON.parse, and throws `OllamaInvariantError("...truncated mid-think...")` if `<think>` opens without a close. (2) `DISABLE_THINKING_MODE=true` is an opt-in env that sets `body.think = false` ONLY when the model matches `/^qwen3/i` — no auto-detect, escape hatch only. (3) Per-stage `num_predict` caps: seeder 3000, parseBrief 2500, analyseGaps 4000, compose 6000 — generous enough that thinking + structured output fit, tight enough that runaway prose can't burn budget. (4) Sharper seeder system prompt: `"You are a fashion-brand copywriter. Write body copy in the requested locale: prose only, no headings, no markdown, no preamble. Length: 150-250 words."` replacing the per-voice template strings (variation now comes from temperature + topic + reserved-vs-random, which is sufficient). (5) Seeder default `--count` restored to 8 per locale (24 total across en-gb/fr-fr/de-de) — had drifted to 40 between phases.
**Alternatives considered:** Auto-disable thinking on qwen3 (rejected — user explicitly said "qwen can think, ACCOMMODATE it"; disabling thinking forfeits the calibration headroom qwen3 was chosen for); ditch qwen3 for a non-thinking model (rejected — qwen3.5:9b is the project-wide chat default since 15a-fix and the team's hardware tier expects it); cap `num_predict` globally in `chat.js` (rejected — different stages need different headroom; seeder wants 3000 for a 250-word output while compose wants 6000 for a 4-6 section JSON outline); leak `<think>` through to downstream parsers and let each parser handle it (rejected — `parseBrief`/`analyseGaps`/`compose` all use Ollama's `format: "json"` which is supposed to enforce JSON-only and would either reject the think prefix or include it in the JSON string; one stripper at the LLM boundary is cleaner); make the stripper match anywhere in the content (rejected — only the LEADING block is reasoning leakage, anything later is part of the answer).
**Consequences:** Re-running `npm run seed -- --seed=20260626` should now produce 24 fragments at ~200 words each instead of 1 runaway + 1 truncated kill. Each pipeline stage has a budget aligned to its output shape. Friendlier mid-seed error: when the loop catches `OllamaInvariantError`/`OllamaJsonParseError`, it logs `"Seed failed at fragment N/M for locale X. Likely cause: thinking-mode output truncation. Recovery options: (a) re-run the same command, or (b) DISABLE_THINKING_MODE=true npm run seed -- --seed=<seed>"` before rethrowing. Tests: +5 in `shared/test/llm.test.js` (num_predict-flowthrough, think-stripper happy path, truncated-mid-think, DISABLE_THINKING_MODE=true sets body.think=false on qwen3, DISABLE_THINKING_MODE unset leaves body.think undefined); +1 in `content-seeder/test/seed.test.js` (default count is 8). Total: 152 → 158 green.

## 2026-06-26 — Phase 15a-fix: swap chat default to `qwen3.5:9b`, raise alpha timeout, retry `parseBrief` twice, harden alpha-run helpers
**Context:** The first `npm run alpha` against `gemma4:26b` produced 0 ok / 6 timeout / 2 error across 20 briefs on the dev host. Root causes: (a) `gemma4:26b` exceeds the locked 120 s chat timeout on consumer hardware, (b) `parseBrief` retried only once on schema drift — not enough margin for a smaller, faster model that occasionally returns prose around the JSON, (c) the alpha runner had no upfront check that `data/corpus.json` was actually seeded with all three locales, (d) the runner couldn't infer a locale for the `winter-sustainable.txt` brief because its filename has no `<locale>-` prefix, and (e) `stageDurations` were lost on the failure path because the partial timing collector lived inside the `try` block.
**Decision:** Five surgical fixes, no scope creep beyond the alpha-run reliability brief. (1) Swap `config/models.json` chat slots to `qwen3.5:9b` and document `gemma4:26b` as a "premium alternative" in README / docs/architecture / docs/prompt-log / docs/sample-run / eval/README. (2) Make `shared/src/llm/chat.js` honour `CHAT_TIMEOUT_MS` at call time so `scripts/alpha-run.js` can pre-set `process.env.CHAT_TIMEOUT_MS ??= "300000"` without touching the runtime default (still 120 000 ms for `npm run agent` / `npm run eval`). (3) `discovery-agent/src/pipeline/parseBrief.js` now retries up to twice on `ZodError` / `OllamaJsonParseError`; the second retry adds `"Return ONLY valid JSON. No prose, no markdown fences, no comments."` to the system prompt. (4) `scripts/alpha-run.js` gains `corpusPrecheck()` (fails fast with an actionable message when `data/corpus.json` is missing / has < 24 fragments / lacks `en-gb|fr-fr|de-de`) and `inferLocale(slug, briefText, expectationsDir)` (slug prefix → `eval/expectations/<slug>.json#localeOverride` → first locale token in the brief text → `"n/a"`). (5) `runBriefOnce` attaches the partial `stageDurations` to the thrown error so `runBriefWithRetry` can persist whatever timing was captured before the stage threw.
**Alternatives considered:** Pull `gemma4:26b` out of the codebase entirely (rejected — the original spec calibrates evaluation expectations against it and graders with the headroom should still be able to opt in); raise the global `CHAT_TIMEOUT_MS` default to 300 000 ms (rejected — would silently slow every agent/eval invocation; the alpha runner is the only stage that should choose 5 min); retry `parseBrief` 3+ times (rejected — by the third call the model is either fundamentally wrong about the brief or the bug is upstream of `parseBrief`; more retries just inflate wall-clock); make `corpusPrecheck` self-heal by running the seeder (rejected — seeding mutates `data/embeddings.db` and rewrites `data/corpus.json` with a different timestamp; the user must own that decision); embed locale-inference logic inside `parseBrief` (rejected — `parseBrief` should not know about filename conventions; the alpha-run script owns the slug↔locale mapping).
**Consequences:** A second `npm run alpha` against the same hardware should now flip the 0/6/2 distribution to mostly ok with rare retries; failing briefs still produce a `.meta.json` with partial `stageDurations` so the post-mortem reads cleanly. Cost: one more env var (`CHAT_TIMEOUT_MS`) in the tuning surface; the alpha-run script pre-sets it so most users never see it. `eval/run.js` is unchanged structurally but now picks up the new default automatically via `getChatModel("default")`. Tests added: 4 for `inferLocale`, 3 for `corpusPrecheck`, 1 for stageDurations-on-error in `scripts/test/alpha-run.test.js`; 1 new "succeeds on second retry" case and 1 reworked "fails after 3 attempts" case in `discovery-agent/test/parseBrief.test.js`. Existing test fixtures that hard-coded `gemma4:26b` (preflight, llm errors) were retargeted to `qwen3.5:9b`; `shared/test/config.test.js` uses arbitrary fixture strings and was left untouched.

## 2026-06-27 — Reused-fragment appendix on `AgentOutput` (additive, `schemaVersion` stays `1.0`)
**Context:** The agent's Markdown output linked outline `Reuse:` sections to ids in the top-3 match table, but the table only showed the score and one-line reason. A reviewer or author opening the rendered Markdown could not see the actual body copy a reuse section was citing without cross-referencing the corpus by id. The spec's Timestamped Agent Results section requires "Markdown output includes a final reused-fragments appendix with full content for every outline `Reuse:` fragment id" and "JSON output includes the same reused fragment appendix as structured data".
**Decision:** Extend `AgentOutput` with `reusedFragments: Fragment[]` (uses the existing `shared/src/schema/fragment.js` schema) defaulting to `[]`, populated in `compose.js` from the top-3 retrieval matches keyed by the ids cited in `draftOutline.sections[].fragmentIds` for `kind: "reuse"` sections — de-duplicated, in first-use outline order. Markdown renderer appends a `## Reused Fragments` section after `## Draft Outline` with each fragment's id, title, path, category, locale, brand guidelines, lastModified, and content; each entry gets a stable `#appendix-<id>` anchor while the original `<a id="<id>">` anchors on the top-match table stay intact. Appendix is omitted entirely when there are no reuse sections. `schemaVersion` stays `"1.0"` because the addition is strictly additive and existing consumers reading by field name are unaffected.
**Alternatives considered:** Bump `schemaVersion` to `"1.1"` (rejected — purely additive change, bumping forces every downstream consumer to update version pins for no behavioural reason and the spec explicitly allowed keeping `1.0`); inline full fragment content into the existing `matchedFragments` array (rejected — `matchedFragments` is `[0..3]` and capped by retrieval, but the appendix scope is "outline reuse ids" which is a subset of those three with different ordering and dedup semantics; inlining would conflate retrieval ranking with composition reuse and break the existing `reason` ≤140 char contract); render the appendix inline under each reuse outline section (rejected — duplicates content when the same id is cited by multiple sections, and the spec asks for a single "bottom section"); pull fragments from corpus on-demand at render time (rejected — couples the renderer to the corpus reader, breaks the existing "renderer is a pure view over `AgentOutput`" invariant, and would re-introduce IO into a sync function); include all retrieval `matches` (rejected — out of scope per the task note; the appendix is only ids cited by `reuse` sections, not every retrieved candidate); include gap `partialMatches` ids (rejected — explicitly out of scope; gap partial matches are about coverage analysis, not draft-page composition).
**Consequences:** Authors and reviewers can read the full body copy of every reused fragment without leaving the rendered output. Existing tests that constructed `AgentOutput` literals without `reusedFragments` continue to pass because of the `.default([])`. `eval/run.js`, `scripts/alpha-run.js`, and any downstream JSON consumers gain a new top-level field they can either read or ignore. Cost: ~30 lines of compose + render + schema code, four new tests across two files (schema validation, compose dedup/empty, render appendix + omit), and a slight increase in `--json` payload size proportional to the cited fragment count (typically 1-3 fragments × ~1-3 KB content each).

## 2026-06-27 — Root-level `architecture.md` as the canonical architecture overview
**Context:** Reviewers landing on the repo had to stitch the architecture together from `README.md`, `docs/architecture.md`, `docs/sample-run.md`, and `why.md`. The deeper-dive `docs/architecture.md` exists but is not linked from the repo root and pre-dates the JSON-primary / locale-ladder / brand-filter splits, so a first-time reader could not get a single accurate one-page picture.
**Decision:** Add `architecture.md` at the repo root. It is a one-stop overview keyed to the current code: TOC, four required Mermaid diagrams (system context, pipeline sequence, retrieval/scoring, output composition) plus the seeding flow, plain-language stage notes that cite their source files, the LLM stack table sourced from `config/models.json`, schema-validation contract, optional AEM source mode, prompt-log audit trail, evaluation harness, "why agentic" rationale, and an abbreviated input/output example pulled from `eval/briefs/winter-sustainable.txt` and `docs/sample-run.md`. The deeper `docs/architecture.md` stays as the companion reference.
**Alternatives considered:** Rewrite `docs/architecture.md` in place and link from README only (rejected — the file is buried under `docs/` and the README would still be the de-facto entry point; reviewers expect `architecture.md` at root in modern repos); fold everything into README (rejected — README is already optimised for "install + run"; mixing in architecture dilutes both); generate from code (rejected — overkill for a one-shot interview exercise, and Mermaid hand-authoring keeps the diagrams aligned with the narrative).
**Consequences:** New readers get the full architecture picture from a single root-level file in < 5 min. Cost: one more doc to keep aligned when stage internals change — mitigated by keeping the file scoped to architecture (not behaviour or prompts) and by linking out to `docs/` and `why.md` for the deeper detail. No code, schema, prompt, or config changes were made.

## 2026-06-27 — Timestamped agent result artifacts under `runs/`
**Context:** Both single-brief CLI runs and the full-run harness emitted output to stdout (single) or to latest-overwriting aggregate files (harness: `<slug>.json` / `<slug>.md` / `<slug>.meta.json`). There was no run-to-run history of what the LLM produced for a given brief, so debugging regressions ("why did this brief's outline change?") required re-running the agent without the prior state to compare against. The interview brief also explicitly asks for repeatable, inspectable runs.
**Decision:** On every successful run, persist the rendered output to a timestamped file: `<ISO-timestamp-with-colons-replaced>-<brief-slug>.<md|json>`. Single-agent runs land in `runs/agent/` by default (override via `--results-dir=<path>`); the full-run harness writes per-brief timestamped artifacts into the existing `runs/full-run/` directory **alongside** the aggregates (the aggregates still overwrite latest, the timestamped files accumulate history). Failed runs do not write artifacts. The slug comes from the brief filename basename, falling back to `stdin` when piped. Filename safety is enforced by replacing `:` and `.` in the ISO timestamp and collapsing unsafe characters in the slug.
**Alternatives considered:** Bump the existing aggregate filenames to include a timestamp (rejected — breaks downstream consumers that grep for `<slug>.json`); write artifacts only for the full-run harness (rejected — the interview brief evaluates single-brief runs too, and "ran it twice and they differ" is exactly what we need to show); write to a single append-only log file (rejected — Markdown and JSON do not append cleanly, and per-run files are easier to diff); add a `--no-save` flag (rejected — would let users silently lose history; nobody asked for an opt-out and the cost of one extra file per successful run is negligible); use a Unix timestamp instead of ISO (rejected — ISO is sortable, human-readable, and already used throughout the codebase).
**Consequences:** Every `npm run agent` invocation produces an inspectable artifact next to its peers in `runs/agent/`, and every `npm run full-run` produces both the latest-overwriting aggregates (existing consumers unchanged) and the accumulating per-brief history. The `runs/` directory grows over time — users who care can prune it; CI clones will not see drift because the timestamped files are unique per run. Cost: one new ~40-line helper (`discovery-agent/src/persistResult.js`), small additions to `cli.js` and `scripts/full-run.js`, ten new tests across the CLI and full-run suites, and the new `--results-dir` flag for tmpdir-based testability.

## 2026-06-27 — Add `pnpm-workspace.yaml` for native pnpm workspace config (keep npm workspaces alongside)
**Context:** The repo declared workspaces only via the npm `"workspaces"` field in root `package.json` and shipped a 114-byte stub `pnpm-lock.yaml` with empty `importers: . {}`. `pnpm install` emitted `WARN The "workspaces" field in package.json is not supported by pnpm. Create a "pnpm-workspace.yaml" file instead.` and could not resolve the internal `@aemdisc/shared` dependency from the workspace, so a fresh `pnpm install` failed with `ERR_PNPM_FETCH_404` trying to fetch `@aemdisc/shared` from the public registry.
**Decision:** Add `pnpm-workspace.yaml` at the repo root listing the three packages (`shared`, `content-seeder`, `discovery-agent`) plus `linkWorkspacePackages: true` (required in pnpm 10, which defaults to `false`, so `"@aemdisc/shared": "*"` resolves to the workspace package) and `onlyBuiltDependencies: [better-sqlite3]` (required in pnpm 10 to allow `better-sqlite3`'s native build script, otherwise the binding fails to load and the retrieval tests can't open the sqlite-vec database). Regenerate `pnpm-lock.yaml` via `pnpm install`. Leave root `package.json` byte-identical (the `"workspaces"` field stays for npm compatibility) and leave `package-lock.json` byte-identical so `npm install` keeps working as a fallback.
**Alternatives considered:** Remove the npm `"workspaces"` field and delete `package-lock.json` (rejected — task scope explicitly forbids edits to those files; npm-fallback compatibility is a stated non-goal to give up); rewrite each workspace's `"@aemdisc/shared": "*"` to `"workspace:*"` (rejected — touches every workspace `package.json` and breaks npm workspaces, which uses `"*"` semantics; `linkWorkspacePackages: true` keeps both tools happy with a single config knob); leave native build scripts blocked and document a manual `pnpm approve-builds` step (rejected — `pnpm test` would fail out of the box for any contributor on pnpm, defeating the purpose of adding pnpm support); pin the pnpm settings in a separate `.npmrc` file (rejected — pnpm 10 prefers settings in `pnpm-workspace.yaml`, and a single new file is less surface area than two).
**Consequences:** `pnpm install` now resolves the internal workspace dependency cleanly and produces a real lockfile (~19 KB with per-importer entries) instead of the 114-byte stub. The cosmetic `WARN The "workspaces" field in package.json is not supported by pnpm` continues to appear on every `pnpm install` — accepted as the trade-off for dual-tool compatibility. `pnpm test` and `npm test` produce identical results (181 tests, 178 pass, 3 pre-existing failures in `compose: *` retry tests unrelated to this change). Cost: one new 10-line config file; no edits to existing `package.json`/`package-lock.json` files; no dependency version drift.

## 2026-06-27 — Triage finding: compose retry tests fail because `discovery-agent` resolves a second physical `zod` copy
**Context:** Three tests in `discovery-agent/test/compose.test.js` (lines 106, 133, 191) fail under both `npm test` and `pnpm test`. The retry-on-ZodError path in `discovery-agent/src/pipeline/compose.js:81` (`err instanceof LlmJsonParseError || err instanceof z.ZodError`) silently turns into `false`, so the first malformed-shape call propagates instead of triggering the documented one-retry. The orphan-id retry test (`compose.test.js:133`) and the second-bad-shape rejection test (`compose.test.js:191`) fail for the same identity reason — the latter's `(err) => err instanceof z.ZodError` predicate in the test file also dereferences a different `zod` than the schema threw from.
**Decision (read-only triage only — no code change):** Classify the failure as **zod duplicate-identity (case (a))** and recommend that `discovery-agent` declare its own direct `"zod": "^3.24.1"` dependency (matching `shared`) so both managers resolve a single physical copy via the `discovery-agent/node_modules/zod` shortest-path. Empirical evidence: `import { z } from "zod"` from inside `discovery-agent` resolves to the extraneous `<root>/node_modules/zod` (a real directory left over from the historic `npm install`, modtime 2026-06-26), while `@aemdisc/shared` resolves zod via its pnpm-managed symlink to `<root>/node_modules/.pnpm/zod@3.25.76/node_modules/zod` (modtime 2026-06-27); the constructed `instanceof` returns `false` across them. Latent root cause is commit `95aaa6b chore(discovery-agent): remove unused zod dependency` (Jun 27 12:59 +0200), which made `compose.js`/`compose.test.js` depend on installer-specific hoisting; the pnpm migration in `d7f3d24` (and the `node_modules/zod` leftover from a prior `npm install` co-existing with the pnpm tree) is what makes it manifest now.
**Alternatives considered:** (b) `compose.js` retry control-flow regression — ruled out: source at HEAD is unchanged since `4c0ce6c` and the loop is correct; (c) test/mock shape drift — ruled out: the same chat-mocks produce a valid `DraftOutline` on the second call (test `compose: mixed outline parses` proves the schema accepts that shape); (d) schema shape change — ruled out: `shared/src/schema/output.js` still rejects exactly what the tests intend it to reject (missing `fragmentIds`, orphan id), and the rejection itself is reaching the catch block; (e) any other — none consistent with the empirical `instanceof` mismatch shown via the standalone Node repro. Recommended fix scope is `discovery-agent/package.json` only (add `"zod": "^3.24.1"` under `dependencies`), then `pnpm install` / `npm install` to refresh the workspace's `node_modules`. Fix size: **trivial (≤10 LOC)**.
**Consequences:** Either accept the recommended one-line dependency addition (preferred — restores the documented retry behavior and matches the pre-`95aaa6b` working layout) or accept that compose's retry contract is at the mercy of the installer's hoisting strategy and re-litigate this every time a new package manager touches the tree. Removing the extraneous `<root>/node_modules/zod` does not solve the underlying problem under pnpm — without a declared `zod` dep, `discovery-agent`'s `import "zod"` would then fail outright with `ERR_MODULE_NOT_FOUND` under strict pnpm layout. The cheapest durable fix is the explicit dependency declaration.

## 2026-06-27 — Upgrade `zod` from v3.25.76 to v4.4.3 across both workspaces
**Context:** The prior triage (entry above) identified a `zod` duplicate-identity bug where `discovery-agent` resolved a physically different copy of zod than `@aemdisc/shared`, causing `err instanceof z.ZodError` to silently return `false` in the `compose` retry loop and failing 3 tests under both `npm test` and `pnpm test`. The fix needed both packages to declare and resolve a single physical zod. The user simultaneously requested the upgrade to v4.4.3 (the current zod major) to land both changes in one step, with the constraint of "keep semantics identical" — no schema-level changes if a v3→v4 validator difference (e.g. `z.iso.datetime()` rejecting timestamps that `z.string().datetime()` accepted) would force one.
**Decision:** Add `"zod": "^4.4.3"` to `discovery-agent/package.json` (new) and bump `shared/package.json` from `"^3.24.1"` to `"^4.4.3"` (existing). No source-code changes required. The four v3 APIs used by the codebase — `z.string().datetime()` (in `fragment.js`, `corpus.js`), `.strict()` (in `output.js`), `z.discriminatedUnion` (in `output.js`), `superRefine`/`z.ZodIssueCode.custom` (in `compose.js`), and `z.ZodError` (in `compose.js`/`analyseGaps.js`/`compose.test.js`) — all continue to work in v4 with identical runtime semantics. `z.string().datetime()` is deprecated in favor of the new top-level `z.iso.datetime()` and `.strict()` is deprecated in favor of `z.strictObject()`, but the v4 release notes explicitly state both legacy forms "still exist and work as before" and are not slated for removal. The deprecation cleanup is intentionally out of scope to keep the upgrade purely a dependency bump.
**Alternatives considered:** (1) Land the duplicate-identity fix first as a v3.24.1 pin in `discovery-agent`, then upgrade in a follow-up — rejected: doubles the churn on the lockfiles and the why.md log for no payoff, and the user explicitly asked for v4.4.3 in this task; (2) Migrate `z.string().datetime()` → `z.iso.datetime()` and `.strict()` → `z.strictObject()` in the same change — rejected: the migration guide flags `z.iso.datetime()` as having a slightly different ISO grammar than v3's regex-based `.datetime()`, and the deterministic seeder writes timestamps that happen to round-trip today but the change would be a semantic risk we'd have to validate against every fixture; the task scope explicitly forbids semantic changes; (3) Pin to `zod@4.0.0` instead of `^4.4.3` to minimize surface — rejected: `^4.4.3` is what the user specified and it picks up the bug-fix line within the v4 major; (4) Upgrade only `shared` and re-export `z` from there so `discovery-agent` never imports zod directly — rejected: deeper API change with knock-on effects on `discovery-agent/test/compose.test.js` which imports `z` directly for `z.ZodError`, and the cheapest durable fix (per the triage entry) is a declared `zod` dep on both packages.
**Consequences:** Both `pnpm test` and `npm test` report **181/181 pass** (previously 178/181 with 3 pre-existing duplicate-identity failures). Single physical zod resolves through `node_modules/.pnpm/zod@4.4.3/node_modules/zod` (pnpm) or hoisted `node_modules/zod` (npm). Cost: lockfile churn (`pnpm-lock.yaml` + `package-lock.json` regenerated, both committed) and one `WARN  1 deprecated subdependencies found: prebuild-install@7.1.3` from `better-sqlite3` that is unrelated to this change. Deprecated v3 surface (`z.string().datetime()`, `.strict()`) is left in place to be addressed in a separate cleanup pass when there is willingness to validate the timestamp/ISO grammar change against every fixture.

## 2026-06-27 — Root-artifact audit: keep brief PDF + `runs/full-run/` sample, gitignore `.augment/`
**Context:** A prior task moved `pure-augment-log.md` / `prompt-log.md` from the repo root to `docs/`. A follow-up audit was needed to decide whether the remaining root-level non-code artifacts (`AEM_Content_Discovery_Agent_Brief.pdf`, the `runs/` directory, and the three IDE/workspace metadata directories `.augment/`, `.idea/`, `.intent/`) were intentional or stale clutter.
**Decision:** Keep all currently tracked artifacts; add `/.augment` to the root `.gitignore` (the only missing rule). Per-item: (1) `AEM_Content_Discovery_Agent_Brief.pdf` (448K) — KEEP at root, README §1 explicitly links to it as `[repo root](AEM_Content_Discovery_Agent_Brief.pdf)` and the directory diagram on line 367 anchors it at the root; reviewers expect the original submission brief at the top of a submission repo; (2) `runs/` — KEEP, `git ls-files runs/` shows only the `runs/full-run/<slug>.{json,md,meta.json}` sample plus `seed-summary.json` and `README.md`, which is the documented submission sample (timestamped accumulating files are already correctly ignored via lines 15–17 of `.gitignore`); (3) `author/` — KEEP, fully gitignored via `/author` (line 5), `git ls-files author/` is empty; (4) `.idea/` — KEEP, fully gitignored via `/.idea` (line 6); (5) `.intent/` — KEEP, the two tracked files (`.intent/.gitignore`, `.intent/config.json`) are intentional, the inner `.intent/.gitignore` whitelists exactly those two and `config.json` defines the workspace's pnpm scripts; adding a root ignore would either be a no-op (already-tracked files stay tracked) or would conflict with the intentional whitelist; (6) `.augment/` — ADD `/.augment` to root `.gitignore`, currently only an empty `specialists/` subdir but no rule existed, purely preventive against future leakage.
**Alternatives considered:** Move `AEM_Content_Discovery_Agent_Brief.pdf` to `docs/` to declutter the root (rejected — README §1 lines 5–6 and the directory diagram on line 367 both anchor the file at the root; moving it forces a coordinated README edit and breaks the convention that the original brief is the first file a reviewer sees in a submission repo); untrack `.intent/config.json` via `git rm --cached` (rejected — explicit task guard against bulk-untracking and the file is meaningful repo metadata, not a session export); add `.intent/` to root `.gitignore` (rejected — would either silently conflict with the inner whitelist or do nothing for already-tracked files); flag the dual `package-lock.json` + `pnpm-lock.yaml` lockfiles as bloat (rejected — explicitly out of scope per task, and the prior `2026-06-27 — Add pnpm-workspace.yaml` entry documents the dual-tool compatibility decision).
**Consequences:** One-line addition to `.gitignore` (`/.augment`). No files moved, no files removed, no source-code or dependency changes. `git status --short` after the change shows only `.gitignore` modified. `npm test` continues to report 181/181 pass (the audit touches no code paths). Cost: future contributors running tools that populate `.augment/` will not accidentally commit those files.

